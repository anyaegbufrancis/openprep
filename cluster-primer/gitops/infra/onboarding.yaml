apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-onboarding
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "1"

spec:
  destination:
    name: ''
    namespace: '*'
    server: 'https://kubernetes.default.svc'
  source:
    path: dev/
    repoURL: 'https://github.com/anyaegbufrancis/jnk.git'
    targetRevision: HEAD
    directory:
      recurse: false
  sources: []
  project: default
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
